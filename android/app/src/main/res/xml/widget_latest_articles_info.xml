<?xml version="1.0" encoding="utf-8"?>
<!-- Default 3x2, resizable down to 2x1. The provider picks a layout for the
     actual size: a one-row strip, a single-article card, or a scrolling list. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:targetCellWidth="3"
    android:targetCellHeight="2"
    android:updatePeriodMillis="3600000"
    android:initialLayout="@layout/widget_latest_articles"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewLayout="@layout/widget_latest_articles"
    android:description="@string/widget_description" />
